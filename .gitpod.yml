image:
  file: .gitpod.Dockerfile

tasks:
  - name: Restore & Build
    init: |
      sudo apt install libnss3-tools
      dotnet restore
      dotnet build

vscode:
  extensions:
    - muhammad-sammy.csharp
